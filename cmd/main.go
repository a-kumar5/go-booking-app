package main

import (
	//"fmt"
	"hello"
	//"os"
)

func main() {
	//fmt.Println(hello.Say(os.Args[1:]))
	//var w = [...]int{1, 2, 3}
	//var x = []int{0, 0, 0}
	//y := hello.Do(w, x)
	//fmt.Println(w, x, y)
	// sorting - map examples

	hello.Sort()
}